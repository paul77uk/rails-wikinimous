<% @articles.each do |article| %>
  <div class="mt-3">
    <%= link_to article.title, article_path(article), class: 'h3 text-decoration-none' %>
    <div class="mt-2"><%= article.content %></div>
  </div>
<% end %>
